/**
 * This package contains all the classes for the CustomHashMap project. It includes the {@code CustomHashMap} class,
 * which implements a custom hash map data structure, the {@code Entry} class, which represents a key-value pair in the
 * hash map, the {@code CustomHashMapTester} class, which contains unit and performance tests for the
 * {@code CustomHashMap} class, and the {@code Main} class, which serves as the entry point for the application.
 * 
 * @author  Tyler Mong
 * @version 1.0
 */

package dev.tylermong.customhashmap;